
# JWT Authentication (Login & Token Based Security)

This project demonstrates JWT (JSON Web Token) based authentication for user login.
It implements secure login, token generation, and protected routes using JWT.

JWT allows stateless authentication, meaning the server does not store sessions.

## Security Features

- Password hashing using bcrypt
- Stateless authentication
- Middleware-based route protection